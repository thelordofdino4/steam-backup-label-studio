diff --git a/src/dsp/cpu.h b/src/dsp/cpu.h
--- a/src/dsp/cpu.h
+++ b/src/dsp/cpu.h
@@ -70,2 +70,3 @@
-#if (defined(__SSE2__) || defined(WEBP_MSC_SSE2)) && \
+#if !defined(SBLS_WEBP_GENERIC_ONLY) && \
+    (defined(__SSE2__) || defined(WEBP_MSC_SSE2)) && \
     (!defined(HAVE_CONFIG_H) || defined(WEBP_HAVE_SSE2))
@@ -79,2 +80,3 @@
-#if (defined(__SSE4_1__) || defined(WEBP_MSC_SSE41)) && \
+#if !defined(SBLS_WEBP_GENERIC_ONLY) && \
+    (defined(__SSE4_1__) || defined(WEBP_MSC_SSE41)) && \
     (!defined(HAVE_CONFIG_H) || defined(WEBP_HAVE_SSE41))
@@ -88,2 +90,3 @@
-#if (defined(__AVX2__) || defined(WEBP_MSC_AVX2)) && \
+#if !defined(SBLS_WEBP_GENERIC_ONLY) && \
+    (defined(__AVX2__) || defined(WEBP_MSC_AVX2)) && \
     (!defined(HAVE_CONFIG_H) || defined(WEBP_HAVE_AVX2))
@@ -106,2 +109,3 @@
-#if ((defined(__ARM_NEON__) || defined(__aarch64__)) &&       \
+#if !defined(SBLS_WEBP_GENERIC_ONLY) &&                      \
+    ((defined(__ARM_NEON__) || defined(__aarch64__)) &&       \
      (!defined(HAVE_CONFIG_H) || defined(WEBP_HAVE_NEON))) && \
@@ -112,3 +116,4 @@
-#if !defined(WEBP_USE_NEON) && defined(__ANDROID__) && \
-    defined(__ARM_ARCH_7A__) && defined(HAVE_CPU_FEATURES_H)
+#if !defined(SBLS_WEBP_GENERIC_ONLY) && !defined(WEBP_USE_NEON) && \
+    defined(__ANDROID__) && defined(__ARM_ARCH_7A__) &&             \
+    defined(HAVE_CPU_FEATURES_H)
 #define WEBP_ANDROID_NEON  // Android targets that may have NEON
@@ -122,2 +127,2 @@
-#if defined(_MSC_VER) && \
+#if !defined(SBLS_WEBP_GENERIC_ONLY) && defined(_MSC_VER) && \
     ((_MSC_VER >= 1700 && defined(_M_ARM)) || \
@@ -142,2 +147,3 @@
-#if defined(__mips__) && !defined(__mips64) && defined(__mips_isa_rev) && \
+#if !defined(SBLS_WEBP_GENERIC_ONLY) && defined(__mips__) &&                \
+    !defined(__mips64) && defined(__mips_isa_rev) && \
     (__mips_isa_rev >= 1) && (__mips_isa_rev < 6)
@@ -153,2 +159,3 @@
-#if defined(__mips_msa) && defined(__mips_isa_rev) && (__mips_isa_rev >= 5)
+#if !defined(SBLS_WEBP_GENERIC_ONLY) && defined(__mips_msa) && \
+    defined(__mips_isa_rev) && (__mips_isa_rev >= 5)
 #define WEBP_USE_MSA
@@ -281 +288,11 @@
+#if defined(SBLS_WEBP_GENERIC_ONLY) &&                               \
+    (defined(WEBP_USE_THREAD) || defined(WEBP_USE_SSE2) ||          \
+     defined(WEBP_USE_SSE41) || defined(WEBP_USE_AVX2) ||           \
+     defined(WEBP_USE_NEON) || defined(WEBP_USE_INTRINSICS) ||      \
+     defined(WEBP_ANDROID_NEON) || defined(WEBP_HAVE_NEON_RTCD) ||  \
+     defined(WEBP_USE_MIPS32) || defined(WEBP_USE_MIPS32_R2) ||     \
+     defined(WEBP_USE_MIPS_DSP_R2) || defined(WEBP_USE_MSA))
+#error "SBLS generic-only libwebp build enabled a platform or thread path"
+#endif
+
 #endif  // WEBP_DSP_CPU_H_
